Create Alma Linux VM
##########################################
VM Configuration
  /usr - lvm - 100GB
  /opt  - lvm  - 100GB
OS - Minimal install with development toos and legacy support
############################################
#selinux disable
vim /etc/selinux/config
 disabled
getenforce
setenforce 0
sestatus

#firewall
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload
firewall-cmd --list-all

######################
install

sudo dnf install epel-release -y
dnf groupinstall "Development Tools" -y
dnf install -y gcc glibc glibc-common wget unzip httpd php gd gd-devel perl perl-devel postfix openssl openssl-devel net-snmp net-snmp-utils make 

useradd nagios
groupadd nagcmd
usermod -aG nagcmd nagios
usermod -aG nagcmd apache


cd /usr/src
wget https://assets.nagios.com/downloads/nagioscore/releases/nagios-4.5.9.tar.gz
tar -zxvf nagios...
cd nagios..

./configure --with-command-group=nagcmd
make all
make install
make install-init
make install-daemoninit
make install-groups-users
 make install-commandmode
make install-config
make install-webconf
make install-exfoliation
htpasswd -c /usr/local/nagios/etc/htpasswd.users nagiosadmin

dnf install -y gcc glibc glibc-common make gettext automake autoconf wget openssl-devel net-snmp net-snmp-utils epel-release perl-Net-SNMP
wget https://github.com/nagios-plugins/nagios-plugins/releases/download/release-2.4.12/nagios-plugins-2.4.12.tar.gz
tar -zxvf nagios-pl..........
cd nagios-plug...
./configure --with-nagios-user=nagios --with-nagios-group=nagios
make
make install











